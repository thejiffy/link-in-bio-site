# Copy this file to .env and fill in the real feed URLs.
# In production these are set as Cloudflare Pages environment variables
# instead (Settings > Environment variables), not committed here.
NEWSLETTER_RSS_URL=
PODCAST_RSS_URL=
